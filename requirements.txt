pyaudiowpatch>=0.2.12
google-genai>=0.8.0
keyring>=25.0.0

# Optional (pip install で個別追加):
# faster-whisper>=1.0.0  - ローカルWhisper STT
# webrtcvad>=2.0.10      - より正確な発話区間検出(VAD)
# openai>=1.0            - OpenAI / OpenRouter バックエンド
