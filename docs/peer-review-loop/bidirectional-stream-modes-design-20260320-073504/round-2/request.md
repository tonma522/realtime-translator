以下を日本語でレビューしてください。

観点:
- 仕様の抜け
- バグや回帰リスク
- テスト不足
- 過剰設計
- 完了条件の不備

出力形式:
1. Findings を重要度順に列挙
2. 重大指摘がなければ「重大指摘なし」と明記
3. 最後に3行以内で総評

前回レビュー全文:
## レビュー結果

---

### Findings（重要度順）

---

#### [重大1] `auto` モードの応答フォーマットが未定義

**観点:** 仕様の抜け / 完了条件の不備

「必要に応じてフォーマットを調整する」とあるが、結果表示に「最終採用方向を出す」という要件を満たすために、モデル応答から方向を抽出するフォーマット仕様が設計段階で確定していない。

- 未定義のまま実装を始めると、`controller.py` でのパース処理と `prompts.py` の出力仕様が食い違う可能性がある
- 「未解決事項」にも挙がっているが、これは機能要件（結果表示ラベル）に直結しており、設計フェーズで解決すべき

**提案:** `{ "direction": "en_ja", "text": "..." }` のような最小フォーマットを確定させ、プロンプト仕様とパース処理を同時に設計する。

---

#### [重大2] 既存履歴データとの互換性方針がない

**観点:** バグ / 回帰リスク

履歴エントリに `stream_id` を追加するとあるが、既存データには `stream_id` が存在しない。

- 古い履歴エントリで再翻訳を実行した場合の `stream_id` 解決ロジックが未定義
- 「未解決事項」として残っているが、再翻訳は既存の中核機能であり、これが壊れると即座に回帰になる

**提案:** 設計内で「`stream_id` 欠損時は `listen_en_ja` / `speak_ja_en` にフォールバック」等の方針を明記する。

---

#### [高1] PTT（プッシュ・トゥ・トーク）のモードマッピングが未定義

**観点:** 仕様の抜け

UI設計の「有効ストリーム」に `プッシュ・トゥ・トーク` が維持項目として挙がっているが、PTTがどの仮想ストリーム（`speak_*`）にマッピングされるか、またはマイクモードを共有するかが記述されていない。

---

#### [高2] 設定変更の即時反映 vs 再起動要否が未定義

**観点:** 仕様の抜け

`pc_audio_mode` / `mic_mode` を変更した際に、実行中のキャプチャストリームへの反映タイミングが記述されていない。

- 即時反映の場合、既に処理中のチャンクの `stream_id` との不整合が生じる
- 再起動が必要な場合、UIでの案内が必要

---

#### [高3] 完了条件（Definition of Done）が未定義

**観点:** 完了条件の不備

段階的実装案の7ステップがあるが、各ステップの完了判断基準がない。

- 「既存テストがすべてパスすること」が前提条件として明記されていない
- テスト方針はあるが「どのテストが通れば本機能完了か」の受け入れ基準がない

---

#### [中1] `auto` モードの言語判定品質テストがない

**観点:** テスト不足

テスト方針の `prompts` セクションは「プロンプトに言語判定の指示が含まれること」の構造テストのみ。

- 英日混在発話、短い発話（1〜3単語）、固有名詞のみ、数字のみ等のエッジケースに対する判定品質テストがない
- LLMのプロンプト依存機能であり、モデル変更時の回帰リスクが高い

---

#### [中2] 2フェーズ + Whisper / 外部STT の組み合わせが不明確

**観点:** 仕様の抜け / バグリスク

制御フロー設計で「2フェーズ」「Whisper」「外部STT」が独立して記述されているが、「2フェーズ有効 かつ Whisper使用」時の `auto` モード動作（Phase1でWhisperが出した文字起こしを使ってPhase2で言語判定するのか等）が不明。

---

#### [中3] 不正値正規化の具体的ロジックが未記述

**観点:** 仕様の抜け

「不正なモード文字列は保存時または読込時に既定値へ正規化する」とあるが、「保存時」と「読込時」のどちらで正規化するかが未決定。両方で行う場合の重複処理のリスクがある。

---

#### [低1] 実行中表示と結果確定後の方向ラベルの区別が不明

**観点:** 仕様の抜け

`auto` モードで音声処理中は方向未確定だが、その間のUI表示（スピナー表示中のラベル等）の仕様がない。処理中に「PC音声 同時翻訳」と表示するのか、方向確定まで何も出さないのかが未定義。

---

#### [低2] `auto` モードのレイテンシ要件がない

**観点:** 非機能要件の抜け

`auto` モードではLLMに言語判定を委ねるため、固定方向モードと比べてレイテンシが増加する可能性がある。許容遅延の基準が非機能要件に記載されていない。

---

### 重大指摘あり

重大1（autoフォーマット未定義）・重大2（履歴互換性方針なし）の2件は、実装前に解決しないと中核機能の回帰または設計後戻りが発生するリスクが高い。

---

### 総評

全体的に採用方針の根拠・影響範囲・段階的実装案は整理されており、設計の骨格は妥当。ただし `auto` モードの応答フォーマットと既存履歴との互換性方針が未解決のまま「実装開始」に進むと手戻りコストが大きい。PTTのマッピングと設定変更タイミングも実装前に一行で方針を確定させれば防げる抜けである。

前回の重大指摘要約:
# High Findings Summary
- [High] `auto` モードの応答フォーマットが未定義で、方向抽出とUI表示仕様が固定されていない
- [High] 旧履歴データに仮想ストリームIDがない場合の互換方針が未定義
- [High] PTT がどの `speak_*` モードに従うか未定義
- [High] 実行中のモード変更反映タイミングが未定義
- [High] Definition of Done が未定義

今回すでに反映した修正:
- `auto` 用応答フォーマットを `DIRECTION` / `ORIGINAL` / `TRANSLATION` の3行で固定
- `show_original=false` でも内部では同じ構造化フォーマットを受け取る方針を追加
- 旧履歴エントリのフォールバック規則を `listen -> listen_en_ja`、`speak -> speak_ja_en` と明記
- PTT は常に `mic_mode` から解決した `speak_*` を使うと明記
- 実行中のモード変更は次回 `翻訳開始` 時に反映し、即時反映しないと明記
- Whisper / 外部STT 使用時は 2フェーズを無効化する既存方針を明記
- 不正値正規化は単一ヘルパーを保存前・読込後の両方から呼ぶ方針を明記
- 完了条件を追加し、最低限の確認対象テストを列挙

対象:
# 双方向ソース別翻訳モード設計

作成日: 2026-03-20

## 概要

PC音声とマイクの各入力ソースに対して、以下の3モードを独立に設定できるようにする。

- 英語→日本語
- 日本語→英語
- 同時翻訳

ここでの「同時翻訳」は、1回の音声から両言語を同時出力する意味ではない。入力音声が英語か日本語かを自動判定し、反対言語へ1本だけ翻訳する意味で扱う。

本設計では、既存の `listen` / `speak` をそのまま翻訳方向の識別子として使わず、入力ソースごとに仮想ストリームを導入する。

## 目的

- マイク入力でも英語→日本語を扱えるようにする
- PC音声でも日本語→英語を扱えるようにする
- 各入力ソースで固定方向または自動方向判定を選べるようにする
- 既存の音声キャプチャ構造とUI色分けを大きく崩さずに拡張する

## 要件

### 機能要件

- `PC音声` に対して以下を選択可能にする
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳
- `マイク` に対して以下を選択可能にする
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳
- 既存の `聴く` / `話す` の有効化は維持する
- 同時翻訳時は、話された主要言語を英語または日本語のどちらかとして扱い、反対言語へ翻訳する
- 通常モード、2フェーズ、Whisper、外部STTの各経路で方向指定を扱えること

### 非機能要件

- 既存の履歴、再翻訳、返答アシスト、議事録生成の前提を壊さない
- UI変更は既存レイアウトの延長で理解しやすいものにする
- 方向判定ロジックはプロンプト中心で実現し、ローカル側で複雑な言語判定器を持たない

## 採用方針

仮想ストリーム方式を採用する。

### 仮想ストリーム定義

PC音声:

- `listen_en_ja`
- `listen_ja_en`
- `listen_auto`

マイク:

- `speak_en_ja`
- `speak_ja_en`
- `speak_auto`

この方式では、入力ソースと翻訳ルールを固定ペアにする。既存のプロンプト生成、履歴表示、再翻訳は最終的に仮想ストリームIDを見て動作するように寄せる。

## 他案と不採用理由

### 案1: ソース別モード値を別途持ち、`listen` / `speak` は維持する

- 利点: 既存構造への変更量が少ない
- 不採用理由: `stream_id` 以外の別情報を複数箇所に渡す必要があり、履歴、プロンプト、表示ラベル、再翻訳の責務境界が曖昧になる

### 案2: 入力言語と出力言語を完全に汎用化する

- 利点: 将来拡張性は高い
- 不採用理由: 今回要件に対して抽象化が強すぎ、UIと設定が過剰になる

## UI設計

### 有効ストリーム

既存の以下は維持する。

- `聴く`
- `話す`
- `プッシュ・トゥ・トーク`
- `2フェーズ(STT→翻訳)`
- `原文も表示`

### 設定ウィンドウの追加項目

デバイス設定または翻訳設定付近に、以下のコンボボックスを追加する。

- `PC音声モード`
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳
- `マイクモード`
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳

既定値:

- PC音声: 英語→日本語
- マイク: 日本語→英語

これにより既存利用者の初期挙動を極力維持する。

## 表示設計

### 実行中表示

結果表示には、ソース名と最終的な方向を出す。

例:

- `PC音声 英語→日本語`
- `PC音声 日本語→英語`
- `マイク 英語→日本語`

`同時翻訳` を選択している場合でも、結果表示や履歴には最終採用方向を出す。`同時翻訳` という設定名はUI設定値として使い、出力上は具体方向を優先する。

方向確定前の partial 表示や処理中表示は、`PC音声 同時翻訳` / `マイク 同時翻訳` のようにモード名を表示し、確定後に `英語→日本語` または `日本語→英語` へ置き換える。

### 履歴

履歴エントリは仮想ストリームIDを保持できるようにする。これにより再翻訳時も同じ方向情報を失わない。

既存の履歴エントリ互換のため、仮想ストリームIDが存在しない旧形式データは以下へフォールバックする。

- `listen` -> `listen_en_ja`
- `speak` -> `speak_ja_en`

再翻訳や表示ラベル生成では、まず仮想ストリームIDを優先し、欠損時のみこのフォールバック規則を使う。

## データモデル変更

### 設定値

新規に以下を追加する。

- `pc_audio_mode`
- `mic_mode`

値は以下のいずれかとする。

- `en_ja`
- `ja_en`
- `auto`

不正値正規化は `normalize_translation_mode(value, default)` のような単一ヘルパーで行い、保存前と読込後の両方から同じヘルパーを呼ぶ。これにより JSON に不正値を残しにくくしつつ、既存設定読込時の防御も維持する。

### メタ定義

既存の固定 `STREAM_LANGS` と `_STREAM_META` は、仮想ストリーム単位の辞書へ拡張する。

保持したい情報:

- 入力ソースラベル
- 表示タグ
- 固定方向か自動方向か
- 固定方向時の `src` / `dst`
- 表示用ラベル

## 制御フロー設計

### 起動時

`StartConfig` に以下を追加する。

- `pc_audio_mode`
- `mic_mode`

コントローラー起動時に、入力ソースごとの設定から実際に使う仮想ストリームIDを決定する。

例:

- PC音声 + `en_ja` -> `listen_en_ja`
- PC音声 + `ja_en` -> `listen_ja_en`
- PC音声 + `auto` -> `listen_auto`
- マイク + `en_ja` -> `speak_en_ja`
- マイク + `ja_en` -> `speak_ja_en`
- マイク + `auto` -> `speak_auto`

### 音声チャンク受付

音声キャプチャ自体は従来どおりソース単位で作成する。コールバックで仮想ストリームIDを解決して、以後はそのIDを `ApiRequest` へ渡す。

PTT はマイク入力専用のため、常に `mic_mode` から解決された `speak_*` 仮想ストリームを使う。PTT 専用の別モードは持たない。

### 通常モード

固定方向:

- 仮想ストリームIDから `src` / `dst` を取得し、既存どおり1回の呼び出しでSTT+翻訳を行う

自動方向:

- 音声内容が英語か日本語かを判定し、反対言語へ翻訳するよう明示した専用プロンプトを使う
- 出力には原文と訳文、または訳文のみを返す
- 結果ラベルには最終採用方向を含める

### 2フェーズ

Phase1:

- 自動方向でも固定方向でも、まず文字起こしを行う

Phase2:

- 固定方向では従来どおり `src` / `dst` を指定して翻訳する
- 自動方向では文字起こし結果を見て英語または日本語を判定し、反対言語へ翻訳する専用テキストプロンプトを使う

### Whisper / 外部STT

- STTワーカーへの投入は仮想ストリームID単位にする
- STT後の翻訳処理で仮想ストリームIDに応じた固定方向または自動方向プロンプトを使う
- 既存挙動を維持し、Whisper または外部STT 使用時は `2フェーズ` を無効化する。つまり `Whisper/OpenAI STT/OpenRouter STT + 2フェーズ` の組み合わせは新たに定義せず、STT結果をそのまま翻訳段へ渡す1経路に統一する

## プロンプト設計

### 固定方向

対象:

- `*_en_ja`
- `*_ja_en`

既存の `build_prompt` / `build_stt_prompt` / `build_translation_prompt` / `build_retranslation_prompt` の考え方を維持しつつ、仮想ストリームIDから `src` / `dst` を解決する。

### 自動方向

対象:

- `*_auto`

専用プロンプトで以下を明示する。

- 入力が英語か日本語かを判定すること
- 判定した主要言語が英語なら日本語へ翻訳すること
- 判定した主要言語が日本語なら英語へ翻訳すること
- コードスイッチング時は主要言語を採用すること
- 出力は1つだけ返すこと

モデル応答から方向を確実に抽出できるよう、`auto` 用の応答フォーマットを設計段階で固定する。通常モードと2フェーズ後段のどちらでも、最低限以下の3行を返させる。

```text
DIRECTION: en_ja | ja_en
ORIGINAL: <transcript>
TRANSLATION: <translated text>
```

`show_original=false` の場合でも、内部処理ではこの3行フォーマットを受け取り、UI表示時に `ORIGINAL` 行のみ隠す。これにより `controller.py` のパース処理と `prompts.py` の仕様を固定できる。

固定方向モードは既存フォーマットを維持してよいが、`auto` モードのみは常に上記フォーマットを返す。

## エラーハンドリング

- モード未設定時は既定値にフォールバックする
- 不正なモード文字列は保存時または読込時に既定値へ正規化する
- 自動方向で言語判定不能な場合は、主要言語推定に従うか、失敗時メッセージを返す
- 既存のデバイス未選択、APIキー未設定、依存未導入のエラー処理は維持する
- 実行中に `pc_audio_mode` / `mic_mode` を変更しても、反映は次回 `翻訳開始` 時とする。稼働中キャプチャへの即時反映は行わず、UIには「次回開始時に反映」の案内を出す

## テスト方針

### prompts

- 仮想ストリームIDごとに正しい言語方向が埋め込まれること
- `auto` 用プロンプトに「言語判定して反対言語へ翻訳」が含まれること
- `auto` 用応答フォーマットに `DIRECTION` / `ORIGINAL` / `TRANSLATION` が含まれること

### controller

- ソースごとのモード設定から正しい仮想ストリームIDが選ばれること
- 通常モード、2フェーズ、Whisper、外部STTの各経路で正しいIDが流れること

### config

- `pc_audio_mode` と `mic_mode` の保存・読込ができること
- 不正値が既定値へ正規化されること
- 旧設定ファイルにキーがない場合、PC音声=`en_ja`、マイク=`ja_en` にフォールバックすること

### UI

- 設定ウィンドウのモード選択が初期値を反映すること
- 結果表示ラベルが仮想ストリーム定義に従うこと
- `auto` 実行中は「同時翻訳」表示、確定後は最終方向表示へ切り替わること

## 影響範囲

- `realtime_translator\\constants.py`
- `realtime_translator\\prompts.py`
- `realtime_translator\\controller.py`
- `realtime_translator\\app.py`
- `realtime_translator\\settings_window.py`
- `realtime_translator\\config.py`
- 履歴や再翻訳関連モジュール
- テスト群

## 段階的実装案

1. 仮想ストリーム定義と設定値を追加する
2. コントローラーでモードから仮想ストリームIDを解決する
3. 固定方向プロンプトを仮想ストリーム対応に置き換える
4. 自動方向プロンプトを追加する
5. UIのモード選択を追加する
6. 履歴、再翻訳、表示ラベルを仮想ストリーム対応にする
7. テストを追加し、既存テストを更新する

## 完了条件

- 設定ウィンドウで `PC音声モード` と `マイクモード` を選択できる
- 通常モードで6つの仮想ストリームすべてが正しい方向で動作する
- Whisper / OpenAI STT / OpenRouter STT 経路でも6つの仮想ストリームが正しい方向で翻訳される
- `auto` モードで方向情報が構造化フォーマットから確実に抽出され、UIラベルと履歴に反映される
- 旧履歴エントリを使った再翻訳がフォールバック規則で動作する
- PTT が `mic_mode` に従って動作する
- 既存関連テストと追加テストが通る

最低限の確認対象:

- `tests\\test_prompts.py`
- `tests\\test_controller.py`
- `tests\\test_config.py`
- 必要に応じて履歴・再翻訳・UI系テスト

## 未解決事項

- 履歴の既存データ互換をどこまで保つか
- `auto` モードの短発話や固有名詞中心発話で、どこまで判定精度を求めるか
- `auto` モードの許容レイテンシを固定方向比でどこまで許容するか

## 範囲外

- 英語と日本語以外の多言語対応
- 1つの音声から日本語訳と英語訳の両方を同時出力する機能
- 自動言語判定のローカル実装
