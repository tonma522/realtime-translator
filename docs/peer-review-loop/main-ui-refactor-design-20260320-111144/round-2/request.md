以下を日本語でレビューしてください。

観点:
- 仕様の抜け
- バグや回帰リスク
- テスト不足
- 過剰設計
- 完了条件の不備

出力形式:
1. Findings を重要度順に列挙
2. 重大指摘がなければ「重大指摘なし」と明記
3. 最後に3行以内で総評

前回レビュー全文:
## レビュー結果

---

### Findings（重要度順）

---

#### [高] 1. エラーイベントの分類方法が未定義

**仕様の抜け**

エラーを「ブロッカー」と「実行時エラー」に分けると明記しているが、キューイベント側でどう区別するかが未定義。`_poll_queue()` が受け取る `error` 系イベントのペイロード設計（種別フラグ？例外型で判断？）がなく、実装者が独自に判断せざるを得ない。

---

#### [高] 2. パネル間更新 API の具体性が低い

**バグ・回帰リスク**

「各パネルはメソッドまたは専用更新 API で情報を受け取る」とあるが、インターフェースの定義がない。段階的実装ではこれが最終ステップ（Step 6）のため、Steps 1〜5 の期間は既存の `tk.*Var` 直接参照と新 API が混在する。このオーバーラップ期間に設定値の二重管理・不整合が生じるリスクが高い。

---

#### [高] 3. 段階的実装の各ステップ完了時の回帰判定方法が未定義

**テスト不足**

Step 1〜6 を順に進める構成だが、各ステップの「完了＝既存機能が壊れていない」を何で確認するかが書かれていない。特に Step 2（タイムラインパネル独立）と Step 3（左カラム追加）の間で、状態バーや `_poll_queue()` の配線が中途半端な状態になりやすい。

---

#### [中] 4. 設定変更 → 左カラム要約の即時反映機構が未定義

**仕様の抜け**

「設定変更後に即反映される」と書かれているが、更新契機（ポーリング？ `trace` コールバック？ 設定ウィンドウ閉じ時のコールバック？）が未定義。`TranslatorApp` → 左カラム要約更新の矢印だけがデータフロー図に書かれており、実装根拠がない。

---

#### [中] 5. 状態バー優先順位ロジックの実装所在が曖昧

**仕様の抜け**

コンポーネント境界では `translation_timeline_panel` が「状態バー表示」責務を持つとされているが、優先順位（エラー > PTT録音中 > 翻訳中 > …）を解決するロジックの所在が `translation_timeline_panel` なのか `app.py` なのか明記されていない。複数状態が同時に立った場合の扱いも未定義。

---

#### [中] 6. 未解決事項3点が完了条件に含まれていない

**完了条件の不備**

仕様末尾で「実装着手前に確定対象に含める」と書きながら、完了条件にはそれらの解決を求める項目がない。結果として、以下が未定義のまま実装完了と判定できてしまう。

- 左カラムの方向サマリー／モード要約の粒度
- 開始前ブロッカーの表示 UI 形式（ラベル or カード）
- 右タブの最小幅下限

---

#### [中] 7. 複数エラー同時発生時のテストが未記載

**テスト不足**

状態バーの優先順位は定義されているが、「API キー未設定（ブロッカー）+ 翻訳中に単発失敗（実行時エラー）」が同時に存在する場合のテストシナリオがない。優先順位ロジックの境界ケースを確認する手段がなく、仕様通りの動作保証ができない。

---

#### [低] 8. 補助タブ切替中に翻訳イベントが届いた場合のテストが未記載

**テスト不足**

`translation_done` イベントが `workspace_panel` に届く際、ユーザーが別タブを開いていた場合のタブ状態・スクロール位置・結果反映の動作が未定義でありテストにも含まれていない。

---

#### [低] 9. 完了条件「機能が退行していない」が定性的すぎる

**完了条件の不備**

翻訳・再翻訳・返答アシスト・議事録が退行していないことを、どのシナリオで確認するかが書かれていない。テスト方針のチェックリストとの対応が取れておらず、完了判断が属人的になる。

---

#### [低] 10. キーボード操作のテストが未記載

**テスト不足**

PTT がキーボードトリガーである場合の操作確認、またはタブ切替のキーボード操作が既存通り機能するかの検証が含まれていない。

---

### 重大指摘

あり（上記 1・2・3 が特に重大）

---

### 総評

UI の役割分担と優先順位の考え方は明快で、段階的実装の順序も妥当。ただし、エラーイベントの分類方法とパネル間更新 API の設計が未定義のまま実装に入ると、分割リファクタの中間状態でデータ不整合や配線取りこぼしが顕在化しやすい。未解決事項3点を「着手前に確定」と書きながら完了条件に含めていない点は、検収時の判断基準を曖昧にするため、完了条件への明示的追加を推奨する。

前回の重大指摘要約:
# High Findings Summary

- [High] エラーを `ブロッカー` と `実行時エラー` に分けるだけで、キューイベント上の分類契約が未定義
- [High] パネル間更新 API の具体インターフェースと導入順序が未定義で、中間状態の二重管理リスクがある
- [High] 段階的実装の各ステップごとの回帰判定方法が未定義

## 修正に直結する要点

- `error` 系イベントの payload 契約と分類責務を明文化する
- `app.py` と各パネル間の更新 API を先に定義し、レイアウト移行前からその API に寄せる
- 各ステップの検証ゲートを追加し、中間状態でも回帰確認できるようにする

今回すでに反映した修正:
- `error` 系イベントの `scope / severity / source / message` 契約と UI 分岐ルールを追加
- パネル間更新 API の最小インターフェースを追加し、導入順序を前倒しした
- 状態バー優先順位ロジックを `TranslatorApp` 側の単一リゾルバに固定した
- 設定変更の即時反映方式を `trace` と明示コールバックで定義した
- 段階的実装の各ステップに検証ゲートを追加した
- 境界条件テスト、キーボード操作テスト、別タブ表示中のイベント受信テストを追加した
- 未解決事項 3 点を完了条件側にも反映した

対象:

```diff
diff --git a/docs/superpowers/specs/2026-03-20-main-ui-refactor-design.md b/docs/superpowers/specs/2026-03-20-main-ui-refactor-design.md
index eb4297a..86ac483 100644
--- a/docs/superpowers/specs/2026-03-20-main-ui-refactor-design.md
+++ b/docs/superpowers/specs/2026-03-20-main-ui-refactor-design.md
@@ -255,6 +255,26 @@ partial 表示と確定表示の責務は現行と同様に保つが、主画面
 - `TranslatorApp` -> 左カラム要約更新
 - `TranslatorController` -> 中央タイムライン / 右タブ更新
 
+設定変更の即時反映は `trace` コールバックを第一手段とし、`settings_window` 上で編集している `tk.*Var` の更新に追従して左カラム要約を更新する。`Text` 系のように `trace` を持たない入力は、設定ウィンドウ側で明示コールバックを発火する。設定ウィンドウ close 時の一括同期だけには依存しない。
+
+### パネル間更新 API
+
+画面分割前に、`TranslatorApp` と各パネルの間に最小 API を定義する。少なくとも以下を持つ。
+
+- `main_controls_panel.apply_session_summary(...)`
+- `main_controls_panel.set_blocker(message | None)`
+- `translation_timeline_panel.set_global_status(status_kind, message)`
+- `translation_timeline_panel.on_partial_start(...)`
+- `translation_timeline_panel.on_partial(...)`
+- `translation_timeline_panel.on_translation_done(...)`
+- `translation_timeline_panel.on_runtime_error(...)`
+- `workspace_panel.on_history_entry(...)`
+- `workspace_panel.on_retranslation_result(...)`
+- `workspace_panel.on_assist_result(...)`
+- `workspace_panel.on_local_error(tool_name, message)`
+
+導入順序は、レイアウト差し替え前に `app.py` から各パネル API を呼ぶ形へ寄せることを優先する。移行期間中に `tk.*Var` の直接参照と新 API 呼び出しを混在させるのは `TranslatorApp` 内部に限定し、各パネル間で二重の状態源を持たせない。
+
 ### 実行イベント
 
 キュー処理のハブは `TranslatorApp._poll_queue()` を維持する。ただし、画面反映の責務は各パネルへ委譲する。
@@ -268,6 +288,27 @@ partial 表示と確定表示の責務は現行と同様に保つが、主画面
 - `assist_result` -> `workspace_panel`
 - `status` -> 状態バー更新
 
+### エラーイベント契約
+
+`_poll_queue()` が受け取る `error` 系イベントは、UI 分岐に必要な分類情報を持つ前提で扱う。少なくとも次を満たす。
+
+- `scope`: `session` | `tool`
+- `severity`: `blocker` | `runtime`
+- `source`: `startup` | `translation` | `retranslation` | `assist` | `minutes` | `settings`
+- `message`: ユーザー表示文言
+
+UI 反映ルール:
+
+- `scope=session` かつ `severity=blocker` は左カラムの開始前ブロッカーへ送る
+- `scope=session` かつ `severity=runtime` は中央タイムラインとグローバル状態バー候補へ送る
+- `scope=tool` は右タブ内だけで処理し、グローバル状態バーには昇格させない
+
+既存の単純な `error` イベントを残す場合でも、`TranslatorApp` 内で上記形式へ正規化してから各パネルへ渡す。
+
+### 状態バー優先順位解決
+
+状態バーの優先順位解決ロジックは `TranslatorApp` 側に単一の状態リゾルバとして持たせる。`translation_timeline_panel` は表示専任とし、優先順位判断は持たない。複数状態が同時に立つ場合は、常に `TranslatorApp` の状態リゾルバが `エラー > PTT録音中 > 翻訳中 > 初期化中 > 待機中` の順で 1 つに畳み込んでから `translation_timeline_panel.set_global_status(...)` へ流す。
+
 ## 実装方針
 
 ### 1. レイアウトの枠組みを差し替える
@@ -310,6 +351,7 @@ partial 表示と確定表示の責務は現行と同様に保つが、主画面
 - `再翻訳 / 返答アシスト / 議事録` がタブ切替後も従来どおり動くこと
 - タブ内処理中表示が正しく出ること
 - タブ内エラーが他タブや主状態バーを壊さないこと
+- 別タブ表示中に翻訳イベントや履歴更新が届いても、アクティブタブやスクロール位置が不正化しないこと
 
 ### レイアウト確認
 
@@ -317,6 +359,13 @@ partial 表示と確定表示の責務は現行と同様に保つが、主画面
 - 最小サイズ付近でも主要操作が隠れないこと
 - 右タブ領域が狭すぎず、結果表示も確保できること
 
+### 優先順位と境界条件確認
+
+- `session blocker + tool local error` が同時発生しても、左カラムと右タブで正しく分離表示されること
+- 実行時エラーと `PTT録音中` が競合した場合、状態バー優先順位が崩れないこと
+- PTT のキーボード操作が既存どおり機能すること
+- Notebook のキーボード操作が実用上破綻していないこと
+
 ## 影響範囲
 
 - `realtime_translator\\app.py`
@@ -326,12 +375,45 @@ partial 表示と確定表示の責務は現行と同様に保つが、主画面
 
 ## 段階的実装案
 
-1. 右側ワークスペースを Notebook 化する
-2. 中央タイムライン領域を独立パネル化する
-3. 左カラムを追加し、既存上部トグル群を置き換える
-4. 状態バーを中央上部へ移し、優先順位ロジックを整理する
-5. 開始前エラー表示を CTA 近傍へ移す
-6. パネル間更新 API を整理し、`app.py` の UI 責務を縮小する
+1. パネル間更新 API と状態リゾルバを先に導入する
+2. 右側ワークスペースを Notebook 化する
+3. 中央タイムライン領域を独立パネル化する
+4. 左カラムを追加し、既存上部トグル群を置き換える
+5. 状態バーを中央上部へ移し、開始前エラー表示を CTA 近傍へ移す
+6. `app.py` の UI 責務を縮小し、旧 widget 参照を整理する
+
+## ステップごとの検証ゲート
+
+### Step 1
+
+- `TranslatorApp` から新 API 経由で既存 UI が更新できること
+- 状態バー優先順位リゾルバが単体確認できること
+
+### Step 2
+
+- 再翻訳 / 返答アシスト / 議事録がタブ切替後も従来どおり動くこと
+- タブ切替中に翻訳イベントが届いても、タブ状態が壊れず結果反映できること
+
+### Step 3
+
+- partial / translation_done / runtime error が中央パネル単体へ反映されること
+- 既存の自動スクロールと履歴連携が維持されること
+
+### Step 4
+
+- 左カラムで開始/停止、有効ストリーム、方向サマリー、モード要約が表示されること
+- 設定変更が即時に左カラム要約へ反映されること
+
+### Step 5
+
+- グローバル状態バーが優先順位どおり 1 本化されること
+- 開始前ブロッカーが CTA 近傍へ表示されること
+- `session blocker + tool local error` のような複合ケースでも誤表示しないこと
+
+### Step 6
+
+- 旧 UI 参照が整理され、`app.py` が配線ハブに縮退していること
+- 最終レイアウトと既存機能が完了条件を満たすこと
 
 ## 完了条件
 
@@ -341,7 +423,10 @@ partial 表示と確定表示の責務は現行と同様に保つが、主画面
 - `開始/停止・有効ストリーム・方向サマリー・状態表示` が主画面に残っている
 - `PTT / VAD / 2フェーズ / 原文表示` は主画面では要約表示になっている
 - グローバル状態バーの優先順位が `エラー > PTT録音中 > 翻訳中 > 初期化中 > 待機中` で統一されている
+- `error` 系イベントの分類契約と、各パネル更新 API の責務が spec 上で定義されている
+- 段階的実装の各ステップに対応する検証ゲートが定義されている
 - 既存の翻訳、再翻訳、返答アシスト、議事録機能が退行していない
+- 未解決事項 3 点が実装着手前に確定され、その結果が実装計画へ反映されている
 
 ## リスク
```
