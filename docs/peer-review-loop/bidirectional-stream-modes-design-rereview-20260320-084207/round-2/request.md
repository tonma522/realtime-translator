以下を日本語でレビューしてください。

観点:
- 仕様の抜け
- バグや回帰リスク
- テスト不足
- 過剰設計
- 完了条件の不備

出力形式:
1. Findings を重要度順に列挙
2. 重大指摘がなければ「重大指摘なし」と明記
3. 最後に3行以内で総評

前回レビュー全文:
## レビュー結果

---

### Findings（重要度順）

---

#### 🔴 重大

**[F-1] `auto` モードのエラー時に履歴非保存とするが、ユーザー体験の崩れが未規定**

`DIRECTION` 欠損時は「履歴非保存かつUIにエラー表示」と明記されているが、その後の挙動が未定義。

- エラー後に再試行できるのか
- 音声は破棄されるのか、原文だけ残せるのか
- 連続エラー時の上限や fallback 方針があるか

モデル出力の不安定さを考慮すると、無条件エラー扱いは運用上厳しい。少なくとも「原文は保存し、翻訳なし状態で履歴に積む」等の軽減策の有無を判断すべき。

---

**[F-2] streaming 中の `DIRECTION` パース境界が実装レベルで未確定**

> 先頭行確定後は `TRANSLATION` を逐次表示してよい

とあるが、以下が未定義：

- `DIRECTION:` 行の区切りを「`\n` 受信」で判定するのか、バッファ長で判定するのか
- モデルが `DIRECTION:` 行を複数チャンクに分けて送信した場合のバッファリング実装方針
- `ORIGINAL:` と `TRANSLATION:` の間のテキストをどうスキップするか

プロンプト設計で固定フォーマットを定義したが、実装の肝であるパーサ仕様が設計書に存在しない。回帰バグの温床になりやすい。

---

#### 🟠 中程度

**[F-3] 2フェーズ + Whisper/外部STT の組み合わせ禁止が設定UIレベルで未制御**

> `Whisper/OpenAI STT/OpenRouter STT + 2フェーズ` の組み合わせは新たに定義せず

と明記されているが、UIで両方を有効にできる場合の制御（グレーアウト、警告、強制無効化等）が設計書に記述されていない。既存でも同様の制約があるなら既存UIの仕様を参照する旨の記載が必要。

---

**[F-4] 外部STTが言語情報を返さない場合の `auto` 経路が未規定**

> 外部STT が言語情報を返せる場合、`auto` の方向決定にはその検出言語を優先利用し、LLM に再判定させない

逆に「返せない場合」の経路が空白。LLM プロンプト判定へフォールバックするのか、エラーとするのかが不明。

---

**[F-5] `resolved_direction` が旧履歴エントリに存在しない場合の書き戻し方針が未定義**

フォールバック規則は定義されているが、読み込んだ旧エントリに `resolved_direction` を書き戻す（マイグレーション）かどうか、再翻訳実行後に補完するかどうかが未記載。旧エントリを何度も再翻訳した場合、毎回フォールバックロジックが走るのか確認が必要。

---

**[F-6] PTT のモード選択UIが不明瞭**

> PTT はマイク入力専用のため、常に `mic_mode` から解決された `speak_*` 仮想ストリームを使う

とあるが、設定ウィンドウで `マイクモード` を変更した場合に PTT も同じモードになることをユーザーが理解できるUI説明がない。PTT 専用の設定行が設定ウィンドウに不要であることが利用者に伝わるか検討が必要。

---

#### 🟡 軽微

**[F-7] テスト方針に統合テスト（E2E経路）が存在しない**

現状のテスト方針はすべてユニット（プロンプト文字列検証、controller単体、config読み書き）に留まる。通常モード・2フェーズ・Whisper・外部STTの4経路を「正しいIDが流れること」で確認するとあるが、実際の翻訳出力品質やstreaming表示の正確性を確認する経路テストの記載がない。

---

**[F-8] `normalize_translation_mode` の呼び出しタイミングの片漏れリスク**

> 保存前と読込後の両方から同じヘルパーを呼ぶ

とあるが、設定値を直接書き換えるコードパス（例：UI経由で直接 `config` 辞書を更新する箇所）が既存コードにある場合、ヘルパーを通さずに不正値が残るリスクがある。既存の設定書き込みパスを棚卸しする旨の記述がない。

---

**[F-9] `auto` モードで `show_original=false` 時の内部3行フォーマット受信と表示切り離しが実装依存**

> 内部処理ではこの3行フォーマットを受け取り、UIに表示時に `ORIGINAL` 行のみ隠す

と明記されているが、streaming 中に `ORIGINAL:` 行が表示バッファを経由する瞬間（チラ見え）が発生しないか確認するテストケースがない。

---

**[F-10] 段階的実装案の順序にリスクあり**

ステップ6「履歴・再翻訳・表示ラベルを仮想ストリーム対応にする」がステップ3〜4のプロンプト対応より後になっている。ステップ3〜5の間は `resolved_direction` が履歴に保存されない状態でコードが動くため、中間状態でのデータ不整合リスクがある。履歴スキーマ変更を先行させるか、中間状態の明示的な禁止が必要。

---

### 重大指摘なし（運用上の懸念はF-1が筆頭）

`DIRECTION` 欠損時の完全エラー扱いは実装方針として整合しているが、モデル出力の不確実性を踏まえると運用で問題になる可能性が高い。重大とするかは判断次第だが、**F-1 と F-2 は実装開始前に決定を要する**。

---

### 総評

仮想ストリーム方式の採用理由と範囲外の切り分けは明確で、設計の方向性は妥当。ただし `auto` モードの streaming パーサ仕様（F-2）と `DIRECTION` 欠損時のエラー処理（F-1）は実装フェーズで最も揉める箇所であり、設計書レベルで解像度を上げる必要がある。旧履歴互換（F-5）と外部STT言語情報なし経路（F-4）も小さいが抜けとして要補記。全体的に骨格は整っており、上記を補完すれば実装に進める状態。


前回の重大指摘要約:
# High Findings Summary
- [High] `auto` モードの `DIRECTION` 欠損時に音声や原文をどう扱うかが未定義
- [High] streaming 中の `DIRECTION` / `ORIGINAL` / `TRANSLATION` パース境界が未定義


今回すでに反映した修正:
- DIRECTION 欠損時は音声や原文を破棄せず、	ranslation="" と rror="direction_parse_failed" を持つ不完全履歴として残す方針を追加
- streaming パーサ仕様を 5 ステップで明文化し、先頭行バッファリング、ORIGINAL の非表示、TRANSLATION 開始条件を固定
- 外部STT が言語情報を返さない場合の LLM 判定フォールバックを追加
- 旧形式履歴エントリの esolved_direction をメモリ上で補完する方針を追加
- 2フェーズ無効化の UI 制約、PTT のモード共有説明、統合テスト要件を追記
- 段階的実装案を履歴スキーマ先行に並べ替え

対象:
# 双方向ソース別翻訳モード設計

作成日: 2026-03-20

## 概要

PC音声とマイクの各入力ソースに対して、以下の3モードを独立に設定できるようにする。

- 英語→日本語
- 日本語→英語
- 同時翻訳

ここでの「同時翻訳」は、1回の音声から両言語を同時出力する意味ではない。入力音声が英語か日本語かを自動判定し、反対言語へ1本だけ翻訳する意味で扱う。

本設計では、既存の `listen` / `speak` をそのまま翻訳方向の識別子として使わず、入力ソースごとに仮想ストリームを導入する。

## 目的

- マイク入力でも英語→日本語を扱えるようにする
- PC音声でも日本語→英語を扱えるようにする
- 各入力ソースで固定方向または自動方向判定を選べるようにする
- 既存の音声キャプチャ構造とUI色分けを大きく崩さずに拡張する

## 要件

### 機能要件

- `PC音声` に対して以下を選択可能にする
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳
- `マイク` に対して以下を選択可能にする
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳
- 既存の `聴く` / `話す` の有効化は維持する
- 同時翻訳時は、話された主要言語を英語または日本語のどちらかとして扱い、反対言語へ翻訳する
- 通常モード、2フェーズ、Whisper、外部STTの各経路で方向指定を扱えること

### 非機能要件

- 既存の履歴、再翻訳、返答アシスト、議事録生成の前提を壊さない
- UI変更は既存レイアウトの延長で理解しやすいものにする
- 方向判定ロジックはプロンプト中心で実現し、ローカル側で複雑な言語判定器を持たない
- `auto` モードでも体感速度を優先し、固定方向モードに近い応答感を維持する

## 採用方針

仮想ストリーム方式を採用する。

### 仮想ストリーム定義

PC音声:

- `listen_en_ja`
- `listen_ja_en`
- `listen_auto`

マイク:

- `speak_en_ja`
- `speak_ja_en`
- `speak_auto`

この方式では、入力ソースと翻訳ルールを固定ペアにする。既存のプロンプト生成、履歴表示、再翻訳は最終的に仮想ストリームIDを見て動作するように寄せる。

## 他案と不採用理由

### 案1: ソース別モード値を別途持ち、`listen` / `speak` は維持する

- 利点: 既存構造への変更量が少ない
- 不採用理由: `stream_id` 以外の別情報を複数箇所に渡す必要があり、履歴、プロンプト、表示ラベル、再翻訳の責務境界が曖昧になる

### 案2: 入力言語と出力言語を完全に汎用化する

- 利点: 将来拡張性は高い
- 不採用理由: 今回要件に対して抽象化が強すぎ、UIと設定が過剰になる

## UI設計

### 有効ストリーム

既存の以下は維持する。

- `聴く`
- `話す`
- `プッシュ・トゥ・トーク`
- `2フェーズ(STT→翻訳)`
- `原文も表示`

### 設定ウィンドウの追加項目

デバイス設定または翻訳設定付近に、以下のコンボボックスを追加する。

- `PC音声モード`
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳
- `マイクモード`
  - 英語→日本語
  - 日本語→英語
  - 同時翻訳

既定値:

- PC音声: 英語→日本語
- マイク: 日本語→英語

これにより既存利用者の初期挙動を極力維持する。

Whisper / OpenAI STT / OpenRouter STT のように `2フェーズ` と両立しない STT バックエンドを選んだ場合、UI は既存挙動どおり `2フェーズ` を無効化または強制オフにする。設定ウィンドウ上でも、その制約が分かる表示にする。

## 表示設計

### 実行中表示

結果表示には、ソース名と最終的な方向を出す。

例:

- `PC音声 英語→日本語`
- `PC音声 日本語→英語`
- `マイク 英語→日本語`

`同時翻訳` を選択している場合でも、結果表示や履歴には最終採用方向を出す。`同時翻訳` という設定名はUI設定値として使い、出力上は具体方向を優先する。

方向確定前の partial 表示や処理中表示は、`PC音声 同時翻訳` / `マイク 同時翻訳` のようにモード名を表示し、確定後に `英語→日本語` または `日本語→英語` へ置き換える。

速度優先方針として、`auto` モードでは全文受信完了まで待たない。モデルには `DIRECTION` 行を先頭で返させ、その1行をパースできた時点で方向を確定し、以後の `TRANSLATION` 部分は通常の streaming 表示として流す。つまり `auto` は「方向だけ先に確定し、訳文は遅延なく順次表示する」。

`show_original=false` 時でも、`ORIGINAL` 行は内部バッファでのみ扱い、表示バッファには流さない。streaming 中に原文が一瞬見える挙動は許容しない。

### 履歴

履歴エントリは仮想ストリームIDを保持できるようにする。これにより再翻訳時も同じ方向情報を失わない。

履歴には少なくとも以下を保持する。

- `virtual_stream_id`
- `resolved_direction`
- `original`
- `translation`

既存の履歴エントリ互換のため、仮想ストリームIDが存在しない旧形式データは以下へフォールバックする。

- `listen` -> `listen_en_ja`
- `speak` -> `speak_ja_en`

`resolved_direction` は固定方向モードでは `virtual_stream_id` から導出できる値、`auto` モードではその発話で最終確定した方向を保持する。

再翻訳や表示ラベル生成では、まず `resolved_direction` を優先し、その次に `virtual_stream_id`、どちらも欠損している旧形式データにのみこのフォールバック規則を使う。

旧形式履歴エントリを再翻訳した際は、解決した方向をメモリ上のエントリへ補完して以後の再利用に使う。永続化済み旧データの一括マイグレーションは行わない。

## データモデル変更

### 設定値

新規に以下を追加する。

- `pc_audio_mode`
- `mic_mode`

値は以下のいずれかとする。

- `en_ja`
- `ja_en`
- `auto`

不正値正規化は `normalize_translation_mode(value, default)` のような単一ヘルパーで行い、保存前と読込後の両方から同じヘルパーを呼ぶ。これにより JSON に不正値を残しにくくしつつ、既存設定読込時の防御も維持する。

### メタ定義

既存の固定 `STREAM_LANGS` と `_STREAM_META` は、仮想ストリーム単位の辞書へ拡張する。

保持したい情報:

- 入力ソースラベル
- 表示タグ
- 固定方向か自動方向か
- 固定方向時の `src` / `dst`
- 表示用ラベル

## 制御フロー設計

### 起動時

`StartConfig` に以下を追加する。

- `pc_audio_mode`
- `mic_mode`

コントローラー起動時に、入力ソースごとの設定から実際に使う仮想ストリームIDを決定する。

例:

- PC音声 + `en_ja` -> `listen_en_ja`
- PC音声 + `ja_en` -> `listen_ja_en`
- PC音声 + `auto` -> `listen_auto`
- マイク + `en_ja` -> `speak_en_ja`
- マイク + `ja_en` -> `speak_ja_en`
- マイク + `auto` -> `speak_auto`

### 音声チャンク受付

音声キャプチャ自体は従来どおりソース単位で作成する。コールバックで仮想ストリームIDを解決して、以後はそのIDを `ApiRequest` へ渡す。

PTT はマイク入力専用のため、常に `mic_mode` から解決された `speak_*` 仮想ストリームを使う。PTT 専用の別モードは持たない。

設定UI上でも、PTT は `マイクモード` をそのまま使うことが分かる説明文を添える。PTT 専用の翻訳方向設定は追加しない。

### 通常モード

固定方向:

- 仮想ストリームIDから `src` / `dst` を取得し、既存どおり1回の呼び出しでSTT+翻訳を行う

自動方向:

- 音声内容が英語か日本語かを判定し、反対言語へ翻訳するよう明示した専用プロンプトを使う
- 出力には原文と訳文、または訳文のみを返す
- 結果ラベルには最終採用方向を含める
- 速度優先のため、応答は `DIRECTION` 行を必ず先頭に出し、その後ろに `ORIGINAL` / `TRANSLATION` を続ける

### 2フェーズ

Phase1:

- 自動方向でも固定方向でも、まず文字起こしを行う
- `auto` モードで Whisper / 外部STT を使う場合、STT の言語パラメータは未指定として自動判定に委ねる。`ja` / `en` の固定ヒントは渡さない

Phase2:

- 固定方向では従来どおり `src` / `dst` を指定して翻訳する
- 自動方向では文字起こし結果を見て英語または日本語を判定し、反対言語へ翻訳する専用テキストプロンプトを使う

### Whisper / 外部STT

- STTワーカーへの投入は仮想ストリームID単位にする
- STT後の翻訳処理で仮想ストリームIDに応じた固定方向または自動方向プロンプトを使う
- 既存挙動を維持し、Whisper または外部STT 使用時は `2フェーズ` を無効化する。つまり `Whisper/OpenAI STT/OpenRouter STT + 2フェーズ` の組み合わせは新たに定義せず、STT結果をそのまま翻訳段へ渡す1経路に統一する
- 速度優先のため、Whisper / OpenAI STT / OpenRouter STT が言語情報を返せる場合、`auto` の方向決定にはその検出言語を優先利用し、LLM に再判定させない
- 外部STTが言語情報を返せない場合のみ、既存の `auto` 用 LLM プロンプト判定へフォールバックする

## プロンプト設計

### 固定方向

対象:

- `*_en_ja`
- `*_ja_en`

既存の `build_prompt` / `build_stt_prompt` / `build_translation_prompt` / `build_retranslation_prompt` の考え方を維持しつつ、仮想ストリームIDから `src` / `dst` を解決する。

### 自動方向

対象:

- `*_auto`

専用プロンプトで以下を明示する。

- 入力が英語か日本語かを判定すること
- 判定した主要言語が英語なら日本語へ翻訳すること
- 判定した主要言語が日本語なら英語へ翻訳すること
- コードスイッチング時は主要言語を採用すること
- 出力は1つだけ返すこと

モデル応答から方向を確実に抽出できるよう、`auto` 用の応答フォーマットを設計段階で固定する。通常モードと2フェーズ後段のどちらでも、最低限以下の3行を返させる。

```text
DIRECTION: en_ja | ja_en
ORIGINAL: <transcript>
TRANSLATION: <translated text>
```

速度優先のため、`DIRECTION:` は応答の先頭行に固定し、最初に生成させる。パーサはこの先頭行を受け取った時点で方向を確定し、残りの `TRANSLATION` テキストは全文完了を待たずに逐次表示する。

`show_original=false` の場合でも、内部処理ではこの3行フォーマットを受け取り、UI表示時に `ORIGINAL` 行のみ隠す。これにより `controller.py` のパース処理と `prompts.py` の仕様を固定できる。

固定方向モードは既存フォーマットを維持してよいが、`auto` モードのみは常に上記フォーマットを返す。

`*_auto` の再翻訳では、履歴に保存された `resolved_direction` を用いて固定方向の再翻訳として扱う。つまり再翻訳時に再度言語判定は行わない。`resolved_direction` が欠損している旧データのみ、旧フォールバック規則または `virtual_stream_id` から方向を解決する。

再翻訳プロンプト設計も同様で、`*_auto` 専用の再翻訳フォーマットは持たない。再翻訳時は `resolved_direction` に応じて `en_ja` または `ja_en` の固定方向 `build_retranslation_prompt` を使う。

streaming 応答では、`DIRECTION` の確定は先頭行の受信完了かつパース成功時点とする。トークン途中や行途中では方向確定しないが、先頭行確定後は `TRANSLATION` を逐次表示してよい。

パーサ仕様は次の通りとする。

1. 受信テキストは先頭改行まで専用バッファへ蓄積する
2. 先頭行が `DIRECTION: en_ja` または `DIRECTION: ja_en` と完全一致した場合のみ方向確定とみなす
3. 方向確定後は `ORIGINAL:` 行を内部用にだけ取り込み、UIには出さない
4. `TRANSLATION:` プレフィックスを検出した時点で、その後続テキストを streaming 表示へ流す
5. 先頭行が不正、または `TRANSLATION:` セクションへ遷移できない場合はフォーマット逸脱エラーとする

## エラーハンドリング

- モード未設定時は既定値にフォールバックする
- 不正なモード文字列は保存時または読込時に既定値へ正規化する
- 自動方向で主要言語を決定できない、または構造化応答を正しく返せない場合はエラーとして扱う
- 既存のデバイス未選択、APIキー未設定、依存未導入のエラー処理は維持する
- 実行中に `pc_audio_mode` / `mic_mode` を変更しても、反映は次回 `翻訳開始` 時とする。稼働中キャプチャへの即時反映は行わず、UIには「次回開始時に反映」の案内を出す

`auto` の構造化応答で `DIRECTION` が欠損または `en_ja` / `ja_en` 以外だった場合は、推測補完せずエラーとして扱う。ただし音声や文字起こし結果は破棄せず、`translation=""` と `error="direction_parse_failed"` を持つ不完全履歴として残し、UI にはフォーマット逸脱エラーを表示する。これにより再確認や手動再翻訳の余地を残す。

## テスト方針

### prompts

- 仮想ストリームIDごとに正しい言語方向が埋め込まれること
- `auto` 用プロンプトに「言語判定して反対言語へ翻訳」が含まれること
- `auto` 用応答フォーマットに `DIRECTION` / `ORIGINAL` / `TRANSLATION` が含まれること
- `*_auto` の再翻訳プロンプトが `resolved_direction` を使う固定方向再翻訳へ切り替わること

### controller

- ソースごとのモード設定から正しい仮想ストリームIDが選ばれること
- 通常モード、2フェーズ、Whisper、外部STTの各経路で正しいIDが流れること
- `auto` で確定した `resolved_direction` が履歴へ保存され、再翻訳時に再利用されること
- `auto` で `DIRECTION` パースに失敗した場合、履歴へ保存せずエラー通知されること
- `auto` で先頭 `DIRECTION` 行受信後に、`TRANSLATION` が streaming 表示へ移行すること
- 外部STT が言語情報を返す場合、`auto` でその言語情報を優先して `resolved_direction` を決めること
- 外部STT が言語情報を返さない場合、`auto` で LLM 判定へフォールバックすること
- 旧形式履歴エントリの再翻訳時に、フォールバック規則で方向解決され、メモリ上の `resolved_direction` が補完されること

### config

- `pc_audio_mode` と `mic_mode` の保存・読込ができること
- 不正値が既定値へ正規化されること
- 旧設定ファイルにキーがない場合、PC音声=`en_ja`、マイク=`ja_en` にフォールバックすること

### UI

- 設定ウィンドウのモード選択が初期値を反映すること
- 結果表示ラベルが仮想ストリーム定義に従うこと
- `auto` 実行中は「同時翻訳」表示、確定後は最終方向表示へ切り替わること
- 実行中にモード変更した場合、「次回開始時に反映」の案内が表示されること
- `auto` でも方向確定後は訳文が逐次表示され、全文受信まで無表示にならないこと
- PTT が `マイクモード` を共有していることが設定UI上で分かること

### 品質検証

- `auto` モード向けに、短発話、固有名詞中心、英日混在、数字中心の代表ケースを少なくともプロンプト/パーサ単位で回帰確認できること

### 統合テスト

- 通常モード、2フェーズ、Whisper、外部STT の主要4経路について、少なくとも1本ずつ end-to-end に近い統合テストを持つこと
- `auto` streaming で `DIRECTION` 確定後に `TRANSLATION` だけが表示され、`ORIGINAL` がチラ見えしないこと

## 影響範囲

- `realtime_translator\\constants.py`
- `realtime_translator\\prompts.py`
- `realtime_translator\\controller.py`
- `realtime_translator\\app.py`
- `realtime_translator\\settings_window.py`
- `realtime_translator\\config.py`
- 履歴や再翻訳関連モジュール
- テスト群

## 段階的実装案

1. 仮想ストリーム定義と設定値を追加する
2. 履歴スキーマと再翻訳用の方向保持を先に追加する
3. コントローラーでモードから仮想ストリームIDを解決する
4. 固定方向プロンプトを仮想ストリーム対応に置き換える
5. 自動方向プロンプトと streaming パーサを追加する
6. UIのモード選択と制約表示を追加する
7. 表示ラベル、エラー表示、再翻訳導線を仮想ストリーム対応にする
8. テストを追加し、既存テストを更新する

## 完了条件

- 設定ウィンドウで `PC音声モード` と `マイクモード` を選択できる
- 通常モードで6つの仮想ストリームすべてが正しい方向で動作する
- Whisper / OpenAI STT / OpenRouter STT 経路でも6つの仮想ストリームが正しい方向で翻訳される
- `auto` モードで方向情報が構造化フォーマットから確実に抽出され、UIラベルと履歴に反映される
- `auto` モードで先頭 `DIRECTION` 行受信後に訳文 streaming が継続され、速度劣化を最小化できる
- 旧履歴エントリを使った再翻訳がフォールバック規則で動作する
- PTT が `mic_mode` に従って動作する
- `auto` モードで `DIRECTION` 欠損または不正値の場合、原文保持の不完全履歴を残しつつ UI にエラー表示される
- 既存関連テストと追加テストが通る

最低限の確認対象:

- `tests\\test_prompts.py`
- `tests\\test_controller.py`
- `tests\\test_config.py`
- 必要に応じて履歴・再翻訳・UI系テスト

## 未解決事項

- 履歴の既存データ互換をどこまで保つか
- `auto` モードの短発話や固有名詞中心発話で、どこまで判定精度を求めるか

## 範囲外

- 英語と日本語以外の多言語対応
- 1つの音声から日本語訳と英語訳の両方を同時出力する機能
- 自動言語判定のローカル実装

