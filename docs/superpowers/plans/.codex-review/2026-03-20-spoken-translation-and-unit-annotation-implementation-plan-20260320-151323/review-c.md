[High] `docs/superpowers/plans/2026-03-20-spoken-translation-and-unit-annotation-implementation-plan.md:181` Task 3 の failing test が `annotate_translation()` の `call_count` しか固定しておらず、spec の「`TranslationHistory.append()` の直前で注釈済み文字列を使う」契約を回帰から守れていません。`call_count == 1` でも raw translation を history に保存する実装は通るため、Step 1 の時点で `history` に入った `entry.translation` が `"annotated"` になることを明示的に落とす必要があります。  
[High] `docs/superpowers/plans/2026-03-20-spoken-translation-and-unit-annotation-implementation-plan.md:252` assist / minutes の回帰防止が弱く、計画上のテストは「注釈を呼ばない」ことしか見ていません。これだと `assist_result` / `minutes` の既存ルーティング自体を壊しても通るので、`on_assist_result()` / `on_minutes_result()` まで従来どおり届くこと、または既存の panel/state が更新されることを focused regression に含めないと「既存フローを壊していない」の証明になりません。  
[Medium] `docs/superpowers/plans/2026-03-20-spoken-translation-and-unit-annotation-implementation-plan.md:60` Task 1 の failing test が `build_prompt()` と `build_translation_prompt()` の一部パスしか見ておらず、同じ Task で更新対象にしている `build_retranslation_prompt()` と auto-direction 系の spoken-style 契約を先に落としていません。現状だと 3 つの prompt builder 全体に要件が入っていなくても Task 1 の TDD が成立したように見えてしまいます。  
[Medium] `docs/superpowers/plans/2026-03-20-spoken-translation-and-unit-annotation-implementation-plan.md:110` pure-function 側の TDD が代表ケース寄りで、spec が明示している「離散テーブルの固定」と「partial failure でも本文保持」の回帰面を failing test として先に固定していません。`#400` 以外の代表テーブル値、`mesh` / `micron` の固定行、例外時に元文を返すケース、日付・バージョン・図面番号の除外は、実装後の追加ではなく Task 2 の最初か遅くとも Task 5 の full suite 前に明示的にロックした方が安全です。