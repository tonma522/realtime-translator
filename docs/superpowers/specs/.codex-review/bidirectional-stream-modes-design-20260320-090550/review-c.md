[High] 完了条件の「`auto` モードでも体感速度を優先し、固定方向モードに近い応答感を維持する」「速度劣化を最小化できる`」（L45, L410）が定量基準も測定手順も持っておらず、達成判定不能なので、`DIRECTION` 確定までの許容時間や固定方向比の上限増分など検証可能な閾値へ落としていない点が不足です。  
[High] テスト方針の統合テストが「主要4経路に少なくとも1本ずつ」（L378）に留まっており、完了条件の「通常モードで6仮想ストリームすべて」「Whisper / OpenAI STT / OpenRouter STT 経路でも6仮想ストリームすべて」（L407-L408）を証明できないため、`2ソース × 3モード × 対象経路` の最小カバレッジ行列が欠けています。  
[Medium] `auto` の streaming パーサ仕様は先頭行バッファ、`\\n` / `\\r\\n`、`TRANSLATION:` 遷移失敗まで詳細化されている一方（L306-L318）、テスト方針にはチャンク境界分割、CRLF、`ORIGINAL` 未完了、`TRANSLATION:` 欠落などの異常系がなく、最も壊れやすい parser の回帰検知が不足しています。  
[Medium] 段階的実装案は不完全履歴の生成仕様を Step 5 で入れるのに対し、再翻訳無効化とエラー可視化を Step 7 に分離しており（L328-L332, L399-L401）、途中段階で `direction_parse_failed` エントリが生成されても消費側ガードが未実装な中間不整合を生みます。  
[Medium] 段階的実装案でテスト追加が最後の Step 8 に後置されているため（L402）、Step 1-7 の仮想ストリーム移行中に既存 `listen` / `speak` 前提を壊しても段階ごとに検出できず、各フェーズを緑で閉じる実装順序になっていません。  
[Medium] 完了条件には、`2フェーズ` 非対応 STT バックエンド時の UI 無効化/強制オフ（L111, L249）と、実行中モード変更が次回開始時反映になる制約（L326）が入っておらず、テスト方針にはあるのに完了判定から漏れているため、完了条件の網羅性が不足しています。